#IP Configuration:
sysctl -w net.ipv4.ip_forward=1

ip addr add 10.1.3.2/24 dev eth0
ip addr add 3.2.0.2/30 dev eth1
ip route add default via 3.2.0.1

iptables -A POSTROUTING -t nat -o eth1 -j MASQUERADE